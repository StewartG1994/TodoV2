const projectName = document.getElementById('projectName')
const description = document.getElementById('description')

function validProjectForm () {
    if (projectName.textContent = '' ){console.log('test')}

    else {return true};
}

export {validProjectForm}